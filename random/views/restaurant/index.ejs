<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1><%= category %> Menu</h1>
    <ul>
        <% for(let food of foods){ %>
                <li><a href='/foodies/<%= food._id %>'><%= food.name %></a></li> 
        <% } %>
    </ul>
    <a href="/foodies/new">New Food Item</a>
    <%if(category !== 'All') {%>
    <a href="/foodies">Menu</a>
    <% } %>
</body>

</html>