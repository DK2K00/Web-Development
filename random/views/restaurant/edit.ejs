<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h1>Edit Products</h1>
    <form action="/foodies/<%= food._id %>?_method=PUT" method="POST">
        <label for="name">Food name</label>
        <input type="text" name="name" id="name" placeholder="Food name" value="<%= food.name %> ">

        <label for="price">Price</label>
        <input type="text" name="price" id="price" placeholder="price" value="<%= food.price %> ">

        <label for="quantity">Quantity</label>
        <input type="text" name="quantity" id="quantity" placeholder="quantity" value="<%= food.quantity %> ">

        <select name="category" id="category">
            <% for(let category of categories){ %>
                <option value="<%=category%>" <%= food.category === category ? 'selected': '' %>><%=category%></option>
                <% } %>
        </select>
        <button>Submit</button>
    </form>

    <a href="/foodies/<%= food._id %> ">Cancel</a>
</body>
</html>